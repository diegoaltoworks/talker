name: Security Scan (disabled)

# Temporarily disabled — TruffleHog and audit integration issues to resolve
on:
  workflow_dispatch:
    inputs:
      placeholder:
        description: "This workflow is temporarily disabled"
        required: false
