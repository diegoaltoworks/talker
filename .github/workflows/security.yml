# Disabled temporarily — TruffleHog and npm audit integration issues to resolve
# name: Security Scan
#
# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main, develop]
#   schedule:
#     - cron: '0 0 * * 1'
#   workflow_dispatch:
